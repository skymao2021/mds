import type { NapiResolveOptions as ResolveOptions } from 'rspack-resolver';
import type { NewResolver } from './types';
export type NodeResolverOptions = {
    extensions?: string[];
    conditionNames?: string[];
    mainFields?: string[];
} & ResolveOptions;
export declare function createNodeResolver({ extensions, conditionNames, mainFields, ...restOptions }?: NodeResolverOptions): NewResolver;
